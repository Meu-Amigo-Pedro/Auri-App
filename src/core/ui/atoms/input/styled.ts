import styled from "styled-components";

export const InputContainer = styled.input`
  outline: none;

  width: 37.2rem;
  max-width: 100%;

  padding: 1rem 1.5rem;

  font-family: Public Sans, sans-serif;
  font-size: 1.6rem;
  line-height: 2.4rem;
  
  &::placeholder {
    color: #637887;
  }

  border-radius: .8rem;
  border: .1rem solid #637887;
`